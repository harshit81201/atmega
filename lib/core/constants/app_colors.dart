import 'package:flutter/material.dart';

/// Application color constants
/// Used throughout the app for consistent theming
class AppColors {
  // Prevent instantiation
  AppColors._();

  /// Primary green color used for success states and main branding
  static const Color primaryGreen = Color(0xFF00C851);
  
  /// Success green color for confirmation screens
  static const Color successGreen = Color(0xFF28A745);
  
  /// White color for text and backgrounds
  static const Color white = Color(0xFFFFFFFF);
  
  /// Light grey for subtle text
  static const Color lightGrey = Color(0xFF9E9E9E);
  
  /// Background grey for cards and containers
  static const Color backgroundGrey = Color(0xFFF5F5F5);
  
  /// Dark grey for primary text
  static const Color darkGrey = Color(0xFF424242);
}